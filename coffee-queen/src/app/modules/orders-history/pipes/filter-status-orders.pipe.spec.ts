import { FilterStatusOrdersPipe } from './filter-status-orders.pipe';

describe('FilterStatusOrdersPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterStatusOrdersPipe();
    expect(pipe).toBeTruthy();
  });
});
