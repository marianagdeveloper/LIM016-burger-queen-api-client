import { FilterStatusAdminPipe } from './filter-status-admin.pipe';

describe('FilterStatusAdminPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterStatusAdminPipe();
    expect(pipe).toBeTruthy();
  });
});
